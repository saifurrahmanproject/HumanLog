# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HumanLog' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HumanLog

       pod 'Firebase'
       pod 'Firebase/Auth' 
  target 'HumanLogTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HumanLogUITests' do
    # Pods for testing
  end

end
